This directory contains valid Lean proofs from the **DSP+** project.

For example, a proof file might be located at: `mathlib4/Proof/imo_2019_p1.lean`.

To build this directory, run the following command in the parent directory:

```bash
lake build Proof
```
